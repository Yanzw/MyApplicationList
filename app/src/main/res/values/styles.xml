<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <!--    android:textColor="@android:color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/btn_margin_left_right"
        android:layout_marginRight="@dimen/btn_margin_left_right"
        android:layout_marginTop="@dimen/btn_margin_top"
        android:background="@drawable/btn_background_selector"
        android:gravity="center"-->

    <style name="BtnStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/btn_margin_left_right</item>
        <item name="android:layout_marginRight">@dimen/btn_margin_left_right</item>
        <item name="android:layout_marginTop">@dimen/btn_margin_top</item>
        <item name="android:background">@drawable/btn_background_selector</item>
        <item name="android:gravity">center</item>
    </style>


</resources>
